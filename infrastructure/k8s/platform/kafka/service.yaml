apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: bun-playground
  labels:
    app.kubernetes.io/name: kafka
    app.kubernetes.io/part-of: bun-playground
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: kafka
  ports:
    - port: 9092
      targetPort: 9092
      protocol: TCP
